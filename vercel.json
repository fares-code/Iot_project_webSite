{
  "version": 2,
  "builds": [
    {
      "src": "server.js", // Path to your Express server file
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)", 
      "dest": "/server.js" 
    },
    {
      "src": "/(.*)", 
      "dest": "/server.js"
    }
  ]
}
